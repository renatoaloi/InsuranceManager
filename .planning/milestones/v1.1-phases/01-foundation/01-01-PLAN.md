---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - InsuranceManager.sln
  - src/InsuranceManager.Api/InsuranceManager.Api.csproj
  - src/InsuranceManager.Domain/InsuranceManager.Domain.csproj
  - src/InsuranceManager.Application/InsuranceManager.Application.csproj
  - src/InsuranceManager.Infrastructure/InsuranceManager.Infrastructure.csproj
autonomous: true
requirements:
  - PERS-02
must_haves:
  truths:
    - Solution has 4 projects following hexagonal architecture (Api, Domain, Application, Infrastructure)
    - Domain layer has no dependencies on infrastructure
    - All project references follow hexagonal pattern (Domain ← Application ← Infrastructure ← Api)
  artifacts:
    - path: "InsuranceManager.sln"
      provides: "Solution file"
    - path: "src/InsuranceManager.Domain/InsuranceManager.Domain.csproj"
      provides: "Domain layer (no external deps)"
    - path: "src/InsuranceManager.Application/InsuranceManager.Application.csproj"
      provides: "Application layer with Domain reference"
    - path: "src/InsuranceManager.Infrastructure/InsuranceManager.Infrastructure.csproj"
      provides: "Infrastructure layer (EF Core, SQLite)"
    - path: "src/InsuranceManager.Api/InsuranceManager.Api.csproj"
      provides: "API layer (minimal, references Application)"
  key_links:
    - from: "InsuranceManager.Application"
      to: "InsuranceManager.Domain"
      via: "project reference"
      pattern: "Domain\\.csproj"
    - from: "InsuranceManager.Infrastructure"
      to: "InsuranceManager.Domain"
      via: "project reference"
      pattern: "Domain\\.csproj"
    - from: "InsuranceManager.Api"
      to: "InsuranceManager.Application"
      via: "project reference"
      pattern: "Application\\.csproj"
---

<objective>
Set up .NET 10 solution with hexagonal architecture projects

Purpose: Create foundation with proper layer separation following hexagonal architecture. Domain layer must have zero external dependencies.
Output: Solution with 4 projects (Api, Domain, Application, Infrastructure) with correct references
</objective>

<execution_context>
@C:/dev/INDT/InsuranceManager/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/INDT/InsuranceManager/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 1 success criteria)
@.planning/REQUIREMENTS.md (PERS-02 requirement)

# Key constraints from PROJECT.md:
- Stack: .NET 10, C# 12
- Arquitetura: Hexagonal / Ports & Adapters / DDD
- Persistência: Banco agnóstico — adapter SQLite apenas por enquanto
</context>

<tasks>

<task type="auto">
  <name>Task: Create .NET solution and 4 hexagonal projects</name>
  <files>
    - InsuranceManager.sln
    - src/InsuranceManager.Api/InsuranceManager.Api.csproj
    - src/InsuranceManager.Domain/InsuranceManager.Domain.csproj
    - src/InsuranceManager.Application/InsuranceManager.Application.csproj
    - src/InsuranceManager.Infrastructure/InsuranceManager.Infrastructure.csproj
  </files>
  <read_first>
    - None (empty project)
  </read_first>
  <action>
Create the .NET 10 solution and 4 class library projects using dotnet CLI:

1. Create solution: `dotnet new sln -n InsuranceManager`

2. Create projects:
   - `dotnet new classlib -n InsuranceManager.Domain -o src/InsuranceManager.Domain --framework net10.0`
   - `dotnet new classlib -n InsuranceManager.Application -o src/InsuranceManager.Application --framework net10.0`
   - `dotnet new classlib -n InsuranceManager.Infrastructure -o src/InsuranceManager.Infrastructure --framework net10.0`
   - `dotnet new webapi -n InsuranceManager.Api -o src/InsuranceManager.Api --framework net10.0`

3. Add to solution: `dotnet sln add src/**/*.csproj`

4. Set project references (hexagonal pattern):
   - Application references Domain
   - Infrastructure references Domain
   - Api references Application (and optionally Infrastructure for DI)

5. In Domain project, delete default Class1.cs (no files needed in Domain layer)

6. Add C# 12 features in all .csproj files:
   ```xml
   <PropertyGroup>
     <LangVersion>12.0</LangVersion>
     <Nullable>enable</Nullable>
   </PropertyGroup>
   ```
  </action>
  <verify>
    <automated>dotnet sln list | Select-String -Pattern "InsuranceManager"</automated>
  </verify>
  <done>
    Solution has 4 projects with correct references. Domain has no external package dependencies.
  </done>
</task>

<task type="auto">
  <name>Task: Create hexagonal folder structure in each project</name>
  <files>
    - src/InsuranceManager.Domain/Entities/
    - src/InsuranceManager.Domain/ValueObjects/
    - src/InsuranceManager.Domain/Ports/
    - src/InsuranceManager.Domain/Events/
    - src/InsuranceManager.Application/Commands/
    - src/InsuranceManager.Application/Queries/
    - src/InsuranceManager.Application/Services/
    - src/InsuranceManager.Infrastructure/Persistence/
    - src/InsuranceManager.Infrastructure/Adapters/
    - src/InsuranceManager.Api/Controllers/
    - src/InsuranceManager.Api/DTOs/
    - src/InsuranceManager.Api/Middleware/
  </files>
  <read_first>
    - None (empty projects)
  </read_first>
  <action>
Create the hexagonal architecture folder structure:

1. Domain layer:
   - Entities/ (Proposal, Policy)
   - ValueObjects/ (CoverageType, ProposalStatus, AssetToken)
   - Ports/ (IProposalRepository, IPolicyRepository - interfaces)
   - Events/ (ProposalCreated, PolicyCreated - domain events)

2. Application layer:
   - Commands/ (CreateProposalCommand, ContractPolicyCommand)
   - Queries/ (GetProposalQuery, ListProposalsQuery)
   - Services/ (ProposalService, PolicyService - orchestration)

3. Infrastructure layer:
   - Persistence/ (DbContext, Migrations)
   - Adapters/ (ProposalRepository, PolicyRepository - implementations)

4. Api layer:
   - Controllers/ (ProposalsController, PoliciesController)
   - DTOs/ (CreateProposalDto, ProposalResponseDto)
   - Middleware/ (error handling, logging)

Note: Do not create any files yet - just the empty folder structure.
  </action>
  <verify>
    <automated>Get-ChildItem -Path src -Directory -Recurse | Select-Object FullName</automated>
  </verify>
  <done>
    All hexagonal folders created in each layer. No .cs files yet (empty structure).
  </done>
</task>

<task type="auto">
  <name>Task: Configure minimal dependencies in Infrastructure project</name>
  <files>
    - src/InsuranceManager.Infrastructure/InsuranceManager.Infrastructure.csproj
  </files>
  <read_first>
    - src/InsuranceManager.Infrastructure/InsuranceManager.Infrastructure.csproj
  </read_first>
  <action>
Add EF Core SQLite and related packages to Infrastructure project:

1. Add NuGet packages:
   ```bash
   cd src/InsuranceManager.Infrastructure
   dotnet add package Microsoft.EntityFrameworkCore.Sqlite --version 10.0.0
   dotnet add package Microsoft.EntityFrameworkCore.Design --version 10.0.0
   ```

2. Add EF Core 10 to Application project for DbContext:
   ```bash
   cd src/InsuranceManager.Application
   dotnet add package Microsoft.EntityFrameworkCore.Abstractions --version 10.0.0
   ```

3. Add minimal ASP.NET to Api project:
   ```bash
   cd src/InsuranceManager.Api
   dotnet add package Microsoft.AspNetCore.OpenApi --version 10.0.0
   dotnet add package Swashbuckle.AspNetCore --version 7.2.0
   ```

4. Verify Infrastructure project shows only EF Core packages (no Domain dependencies):
   ```bash
   dotnet list src/InsuranceManager.Infrastructure/InsuranceManager.Infrastructure.csproj package
   ```
  </action>
  <verify>
    <automated>dotnet build InsuranceManager.sln --no-restore 2>&1 | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    Solution builds successfully with all dependencies. Infrastructure has EF Core, Domain has no external deps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Api → Application | Untrusted HTTP input enters here |
| Application → Domain | Commands cross to domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Injection | Project references | mitigate | Domain layer has no external deps - no injection vector |
| T-01-01 | Tampering | Project references | mitigate | Hexagonal boundaries prevent unauthorized cross-layer access |
</threat_model>

<verification>
- Run `dotnet build InsuranceManager.sln` - must succeed
- Verify Domain project has no PackageReference (only net10.0 framework)
- Verify project reference chain: Api → Application → Domain
</verification>

<success_criteria>
- [ ] Solution builds without errors
- [ ] 4 projects created with correct hexagonal references
- [ ] Domain layer has no external dependencies
- [ ] Empty folder structure in place for entities, ports, adapters
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>