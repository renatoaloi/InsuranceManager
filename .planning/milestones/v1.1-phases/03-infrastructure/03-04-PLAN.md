---
phase: 03-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - INFR-03
user_setup: []

must_haves:
  truths:
    - "Huey filesystem broker works in Docker environment"
    - "Tasks can be enqueued from API container and dequeued by worker container"
    - "FileHuey uses bind mount, not CIFS/network share"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Verified working with shared volume"
  key_links:
    - from: "docker-compose.yml volumes"
      to: "huey_data directory"
      via: "bind mount ./huey_data:/app/huey_data"
      pattern: "huey_data:/app/huey_data"
---

<objective>
Verify that Huey filesystem broker works correctly in Docker environment.

Purpose: Confirm that FileHuey works with bind mounts (not CIFS), and tasks flow from API to worker.
Output: Verification report (manual checkpoint with automated tests)
</objective>

<read_first>
- docker-compose.yml (created in plan 03-03)
- .planning/phases/03-infrastructure/03-RESEARCH.md (Common Pitfalls: FileHuey CIFS, Volume Mount Path Mismatch)
- .planning/phases/02-status-lifecycle-auth/02-RESEARCH.md (Pattern 2: Huey Enqueue from .NET)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create huey_data directory for bind mount</name>
  <files>huey_data/.gitkeep</files>
  <action>
Create empty huey_data/ directory in project root with .gitkeep to preserve directory in git.
This directory will be bind-mounted to both containers for shared queue storage.
  </action>
  <verify>
<automated>
Test-Path "huey_data"
</automated>
  </verify>
  <done>
huey_data directory exists in project root
</done>
</task>

<task type="auto">
  <name>Task 2: Verify docker-compose configuration</name>
  <files>docker-compose.yml</files>
  <action>
Verify docker-compose.yml has:
1. Bind mount syntax: ./huey_data:/app/huey_data (not named volume)
2. Both API and worker have SAME mount path
3. Worker uses API_BASE_URL=http://api:8080 (Docker service name)
4. API_KEY and INTERNAL_API_KEY come from ${VAR} syntax

Check for common issues:
- Path mismatch: host ./huey_data vs container /app/huey_data must match
- Worker using localhost instead of Docker service name
  </action>
  <verify>
<automated>
if (Test-Path "docker-compose.yml") {
  $content = Get-Content "docker-compose.yml" -Raw
  if ($content -match '\./huey_data:/app/huey_data') { "BIND_MOUNT_OK" } else { "BIND_MOUNT_MISSING" }
} else { "FILE_MISSING" }
</automated>
  </verify>
  <done>
docker-compose.yml verified with correct bind mount syntax
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Docker-based infrastructure with Huey filesystem broker</what-built>
  <how-to-verify>
Steps to verify filesystem broker works in Docker:
1. Ensure Docker Desktop is running
2. Run: docker-compose build (in project root)
3. Run: docker-compose up -d
4. Check API container: docker-compose ps (both services should be running)
5. Check API health: curl http://localhost:5000/health
6. Check Huey worker logs: docker-compose logs insurance-huey
7. To test the full flow:
   - Create a proposal via POST /api/proposals with valid API_KEY header
   - Enqueue a status change via POST /api/proposals/{id}/status
   - Check huey_data/ directory for queue files
   - Verify status changed in database after worker processes task
8. Verify by checking: ls huey_data/ shows queue files being created
  </how-to-verify>
  <resume-signal>Type "approved" if both containers start and Huey processes tasks, or describe issues if verification fails</resume-signal>
</task>

</tasks>

<verification>
1. huey_data directory created for bind mount
2. docker-compose.yml verified for correct bind mount syntax
3. Manual verification: docker-compose build, up, and test end-to-end flow
4. FileHuey uses bind mount (./huey_data:/app/huey_data), not CIFS
</verification>

<success_criteria>
Huey filesystem broker works on Windows and Docker (INFR-03): Verified that bind mount works correctly, tasks flow from API to worker, and FileHuey exclusive locks function properly.
</success_criteria>

<output>
After completion, create .planning/phases/03-infrastructure/03-04-SUMMARY.md
</output>