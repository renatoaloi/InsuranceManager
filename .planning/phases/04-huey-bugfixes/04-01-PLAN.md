---
phase: 04-huey-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Dockerfile.huey]
autonomous: true
requirements: [BUG-01, BUG-02]

must_haves:
  truths:
    - "Huey container starts without errors"
    - "Huey worker connects to filesystem broker and processes tasks"
  artifacts:
    - path: "Dockerfile.huey"
      provides: "Container definition with direct script CMD"
      contains: "CMD [\"python\", \"Huey/huey_consumer.py\"]"
      min_lines: 25
    - path: "huey_data/"
      provides: "Shared queue directory for FileHuey"
      must_exist: true

key_links:
  - from: "Dockerfile.huey"
    to: "huey_consumer.py"
    via: "COPY command"
    pattern: "COPY.*Huey/huey_consumer.py"
  - from: "huey_consumer.py"
    to: "huey_data/"
    via: "FileHuey path"
    pattern: "path=os.environ.get.*HUEY_QUEUE_PATH"
---

<objective>
Fix Huey container startup by correcting Dockerfile.huey CMD and ensuring queue directory exists.
</objective>

<context>
@C:/dev/INDT/InsuranceManager/Dockerfile.huey
@C:/dev/INDT/InsuranceManager/src/InsuranceManager.Application/Huey/huey_consumer.py

Current Dockerfile.huey uses `python -m huey` module approach. Per D-01, must use direct script approach:
```
CMD ["python", "Huey/huey_consumer.py"]
```

The huey_consumer.py has `if __name__ == '__main__':` block that runs Consumer directly (lines 51-55).
</context>

<read_first>
Dockerfile.huey - Current CMD uses `-m huey` which may fail
huey_consumer.py - Has `__main__` block for direct execution
</read_first>

<tasks>

<task type="auto">
  <name>Fix Dockerfile.huey CMD to use direct script</name>
  <files>Dockerfile.huey</files>
  <action>
    Edit Dockerfile.huey line 23:
    - OLD: CMD ["python", "-m", "huey", "-c", "Huey/huey_consumer.py", "worker"]
    - NEW: CMD ["python", "Huey/huey_consumer.py"]
    
    Also add directory creation before USER directive to ensure huey_data exists:
    - After COPY command (line 11), add: RUN mkdir -p /app/huey_data && chown -R 1000:1000 /app/huey_data
    
    Per D-01: Use direct script approach, not module approach.
  </action>
  <verify>
    <automated>grep -c 'CMD.*huey_consumer.py' Dockerfile.huey</automated>
  </verify>
  <done>
    Dockerfile.huey uses direct script CMD and creates huey_data directory on startup
  </done>
</task>

</tasks>

<verification>
## Verification Commands

1. Build Huey container:
   docker build -f Dockerfile.huey -t insurance-huey:test .

2. Run container and check logs:
   docker run --rm insurance-huey:test
   # Should see: "Starting Huey consumer worker..." without errors

3. Check container stays running:
   docker run -d --name huey-test insurance-huey:test
   docker logs huey-test
   # Should show worker polling without crashes

4. Cleanup:
   docker stop huey-test 2>/dev/null; docker rm huey-test 2>/dev/null
</verification>

<success_criteria>
- Dockerfile.huey CMD uses direct script: `python Huey/huey_consumer.py`
- huey_data directory created and owned by worker user
- Container starts without errors and worker polls queue
</success_criteria>

<output>
After completion, create `.planning/phases/04-huey-bugfixes/04-01-SUMMARY.md`
</output>