---
phase: 06-integration-architecture-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ARCH-04
  - ARCH-05

must_haves:
  truths:
    - "All 12 architecture tests pass (Domain isolation + Port/Adapter pattern)"
    - "ROADMAP.md updated - no instances of IHueyTaskRunner or HueyTaskRunnerAdapter remain"
  artifacts:
    - path: ".planning/ROADMAP.md"
      contains: "IQueueTaskAdapter"
      not_contains: "IHueyTaskRunner"
  key_links:
    - from: "ROADMAP.md Phase 5"
      to: "ARCH-01, ARCH-02, ARCH-03"
      via: "naming alignment"
---

<objective>
Verify architecture is correctly implemented and update documentation to reflect actual implementation naming.

Purpose: Phase 6 validates that the hexagonal architecture from Phase 5 is properly wired and Domain layer has zero external dependencies. The architecture is already complete - this phase confirms tests pass and aligns documentation.

Output: REQUIREMENTS.md and ROADMAP.md updated to reflect `IQueueTaskAdapter` naming
</objective>

<execution_context>
@C:/dev/INDT/InsuranceManager/.opencode/get-shit-done/workflows/execute-plan.md
@C:/dev/INDT/InsuranceManager/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-integration-architecture-validation/06-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run architecture tests to verify implementation</name>
  <files>tests/InsuranceManager.Architecture.Tests/DomainLayerIsolationTests.cs
    tests/InsuranceManager.Architecture.Tests/PortAdapterPatternTests.cs</files>
  <action>
Run the architecture test suite to confirm all 12 tests pass. Execute:
```
dotnet test --filter "FullyQualifiedName~Architecture.Tests" --verbosity normal
```

Expected result: 12 passed, 0 failed.

Tests verify:
- Domain layer has no project references (ARCH-05)
- Domain layer has no package references (ARCH-05)
- Domain files have no Infrastructure usings (ARCH-05)
- IQueueTaskAdapter port exists in Domain layer (ARCH-04 related)
- QueueTaskRunnerAdapter implements port in Infrastructure (ARCH-04 related)
- Application services inject port abstraction (ARCH-04)
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~Architecture.Tests" --verbosity minimal 2>&1 | Select-String "Passed!" | Select-String "Aprovado"</automated>
  </verify>
  <done>All 12 architecture tests pass - hexagonal architecture correctly implemented</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 5 to reflect actual implementation naming</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update ROADMAP.md to align with actual implementation naming. The following lines in the Phase 5 section still use IHueyTaskRunner/HueyTaskRunnerAdapter but should use IQueueTaskAdapter/QueueTaskRunnerAdapter:

1. Line 31: "Create IHueyTaskRunner port interface and HueyTaskRunnerAdapter" → "Create IQueueTaskAdapter port interface and QueueTaskRunnerAdapter"
2. Line 41: "HueyTaskRunner class no longer exists" → "HueyTaskRunner class no longer exists" (correct - it's an artifact)
3. Line 42: "IHueyTaskRunner interface exists" → "IQueueTaskAdapter interface exists"
4. Line 43: "HueyTaskRunnerAdapter class exists" → "QueueTaskRunnerAdapter class exists"
5. Line 44: "IHueyTaskRunner abstraction" → "IQueueTaskAdapter abstraction"
6. Line 45: "HueyTaskRunner in Infrastructure" → "QueueTaskRunnerAdapter in Infrastructure"
7. Line 48: "Create IHueyTaskRunner port" → "Create IQueueTaskAdapter port"

Also update Phase 6 success criteria (lines 55, 59) to use IQueueTaskAdapter.
  </action>
  <verify>
    <automated>Select-String -Path ".planning/ROADMAP.md" -Pattern "IHueyTaskRunner" | Measure-Object -Line</automated>
  </verify>
  <done>ROADMAP.md updated - no instances of IHueyTaskRunner remain in Phase 5 section</done>
</task>

<task type="auto">
  <name>Task 3: Verify all ROADMAP.md references are updated</name>
  <files>.planning/ROADMAP.md</files>
  <action>
After Task 2, verify that ROADMAP.md Phase 6 success criteria also reference IQueueTaskAdapter (not IHueyTaskRunner). If any references remain, update them.
  </action>
  <verify>
    <automated>Select-String -Path ".planning/ROADMAP.md" -Pattern "IHueyTaskRunner" | Measure-Object -Line</automated>
  </verify>
  <done>All ROADMAP.md references updated to IQueueTaskAdapter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Update only - no security-sensitive changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Documentation | REQUIREMENTS.md | accept | Documentation update only, no code changes |
</threat_model>

<verification>
Run: `dotnet test --filter "FullyQualifiedName~Architecture.Tests"` - expect 12 passed

Verify REQUIREMENTS.md has no "IHueyTaskRunner" references:
```powershell
Select-String -Path ".planning/REQUIREMENTS.md" -Pattern "IHueyTaskRunner" | Measure-Object -Line
```
Expected: 0 matches

Verify ROADMAP.md has no "IHueyTaskRunner" references:
```powershell
Select-String -Path ".planning/ROADMAP.md" -Pattern "IHueyTaskRunner" | Measure-Object -Line
```
Expected: 0 matches
</verification>

<success_criteria>
Phase 6 complete when:
- [ ] `dotnet test --filter "FullyQualifiedName~Architecture.Tests"` shows 12 passed
- [ ] REQUIREMENTS.md contains "IQueueTaskAdapter" (not "IHueyTaskRunner")
- [ ] ROADMAP.md Phase 6 success criteria reference "IQueueTaskAdapter"
- [ ] ARCH-04 and ARCH-05 marked as Complete in traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-architecture-validation/06-01-SUMMARY.md`
</output>