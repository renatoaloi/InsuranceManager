---
phase: 03-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - INFR-02
user_setup: []

must_haves:
  truths:
    - "Huey worker runs as a separate process/container"
    - "Huey worker has access to shared huey_data directory"
    - "Worker uses Python slim for minimal image size"
  artifacts:
    - path: "Dockerfile.huey"
      provides: "Python container for Huey worker"
      min_lines: 20
  key_links:
    - from: "Dockerfile.huey"
      to: "src/InsuranceManager.Application/Huey/"
      via: "COPY directive"
      pattern: "COPY.*Huey"
    - from: "Dockerfile.huey"
      to: "huey_consumer.py"
      via: "CMD directive"
      pattern: "CMD.*huey_consumer"
---

<objective>
Create a Dockerfile for the Huey worker container.

Purpose: Enable Huey worker to run as a separate container process with access to shared queue directory.
Output: Dockerfile.huey (in project root, alongside docker-compose.yml)
</objective>

<read_first>
- src/InsuranceManager.Application/Huey/huey_consumer.py (consumer script)
- src/InsuranceManager.Application/Huey/huey_config.py (FileHuey config)
- .planning/phases/03-infrastructure/03-RESEARCH.md (Pattern 2: Huey Worker Dockerfile)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile for Huey worker</name>
  <files>Dockerfile.huey</files>
  <action>
Create Dockerfile.huey in project root that:
1. Uses python:3.12-slim as base image
2. Installs huey and requests packages via pip
3. Copies src/InsuranceManager.Application/Huey/ files to /app/Huey/
4. Creates non-root user "worker" for security
5. Sets working directory to /app
6. Default command runs: python Huey/huey_consumer.py

Key configuration:
- RUN pip install --no-cache-dir huey requests
- COPY src/InsuranceManager.Application/Huey/ ./Huey/
- USER worker (after creating with useradd -m worker)
  </action>
  <verify>
<automated>
if (Test-Path "Dockerfile.huey") { Get-Content "Dockerfile.huey" | Select-String -Pattern "huey" | Select-Object -First 1 } else { Write-Output "FILE_MISSING" }
</automated>
  </verify>
  <done>
Dockerfile.huey exists with Python slim, Huey installed, non-root user, consumer command
</done>
</task>

<task type="auto">
  <name>Task 2: Create Python requirements file for Huey</name>
  <files>requirements-huey.txt</files>
  <action>
Create requirements-huey.txt in project root with:
- huey>=3.0.0
- requests>=2.31.0
This provides dependency documentation and can be used for pip install.
  </action>
  <verify>
<automated>
Test-Path "requirements-huey.txt"
</automated>
  </verify>
  <done>
requirements-huey.txt exists with huey and requests dependencies
</done>
</task>

<task type="auto">
  <name>Task 3: Create .dockerignore for Huey worker</name>
  <files>.dockerignore</files>
  <action>
Create .dockerignore in project root to exclude unnecessary files from build context:
- .git/
- src/ (not needed, Huey files copied directly)
- *.md, *.txt (except requirements-huey.txt)
- bin/, obj/
- *.db (SQLite database)
- huey_data/ (created at runtime)
  </action>
  <verify>
<automated>
Test-Path ".dockerignore"
</automated>
  </verify>
  <done>
.dockerignore exists with appropriate exclusions
</done>
</task>

</tasks>

<verification>
1. Dockerfile.huey exists in project root
2. Python 3.12-slim base image
3. Huey and requests installed
4. Non-root user configured
5. Consumer command runs huey_consumer.py
</verification>

<success_criteria>
Huey worker runs as separate process/container (INFR-02): Dockerfile.huey creates a Python container that runs the Huey consumer and processes queued tasks.
</success_criteria>

<output>
After completion, create .planning/phases/03-infrastructure/03-02-SUMMARY.md
</output>